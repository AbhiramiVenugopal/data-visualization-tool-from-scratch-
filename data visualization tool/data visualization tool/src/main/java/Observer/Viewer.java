package Observer;

/*
 * abstract class from each type of visualization 
 * is derived. 
 */
public abstract class Viewer implements Observer {
	//needs to go to the object of each of the graphs created 
	//tell it if it is being displayed or not 
	
	//this means that an object of each graph has to be created as soon 
	//as the system is run - so then what will factory method do?
	
	
}
